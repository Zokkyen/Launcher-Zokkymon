# ── Build Maven ───────────────────────────────────────────────────────────────
target/
*.class
*.war
*.ear
# maven-shade génère ce fichier à côté de pom.xml — ne pas versionner
dependency-reduced-pom.xml

# ── Build Gradle (module splash-mod) ─────────────────────────────────────────
.gradle/
splash-mod/.gradle/
splash-mod/build/

# ── Artefacts binaires générés ────────────────────────────────────────────────
# Générés par Launch4j / Maven package — distribués via GitHub Releases
ZokkymonLauncher.exe
ZokkymonLauncher.jar
# Log Launch4j
launch4j.log

# ── Artefacts locaux launcher (runtime) ───────────────────────────────────────
# Dépendances locales/tampon de test — ne pas versionner
libs/
mods/

# ── IDE ───────────────────────────────────────────────────────────────────────
.idea/
*.iml
.vscode/
*.code-workspace

# ── Secrets & données sensibles ───────────────────────────────────────────────
# config/launcher_config.json contient le modpackToken, le msaClientId réel
# et le installPath local → ne jamais commiter.
# src/main/resources/launcher_config.json est la version vide embarquée dans
# le JAR : elle est safe à commiter (token vide, installPath vide).
config/launcher_config.json

# KeyStore local (clé AES machine) — propre à chaque installation
**/.ks

# ── OS ────────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Logs ──────────────────────────────────────────────────────────────────────
*.log
